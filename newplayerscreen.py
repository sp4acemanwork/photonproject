from window import page
from window import window
import tkinter as tk


class playerscreen(page):
    def __init__(self, parrent_window: tk.Tk, parrent: window):
        self.__init__(parrent_window)

